<form method="post" action="{{ path('app_admin_fichedepaie_delete', {'id': fiche_de_paie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_de_paie.id) }}">
    <button class="btn">Delete</button>
</form>
